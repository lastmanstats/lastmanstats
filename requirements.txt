# World Cup 2026 Content Pipeline — Dipendenze Python
# Testato con Python 3.10+
# Installa con: pip install -r requirements.txt

# HTTP client — richieste API football-data.org
requests==2.32.3

# Parsing feed RSS (fallback notizie calcistiche)
feedparser==6.0.11

# Generazione immagini e testo grafico sui frame video
Pillow==10.4.0

# YouTube Data API v3 — upload automatico
google-api-python-client==2.151.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# Caricamento variabili d'ambiente da file .env (solo locale)
python-dotenv==1.0.1

# OpenAI TTS — voiceover automatico (voce onyx, ~€0.002/video)
# Se OPENAI_API_KEY non è impostata, il modulo genera video senza audio senza errori bloccanti.
openai>=1.35.0
